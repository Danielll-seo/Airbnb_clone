# Backend

This folder contains the Django backend for the Airbnb clone.

- `pyproject.toml` and `poetry.lock` manage dependencies.
- `manage.py` is the Django management entrypoint.
- `airbnb_backend/` contains the Django project configuration.
- `db.sqlite3` is the default local SQLite database.
